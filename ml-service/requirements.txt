# Deliberately minimal.
#
# The collaborative-filtering optimiser is `implicit`'s Cython BPR (Rendle et
# al., 2009). Everything layered around it -- the TF-IDF content scorer, the
# time-decayed popularity scorer, the hybrid combiner and the whole evaluation
# harness -- is bespoke NumPy in app/, so the ranking logic stays readable.
#
# scipy is required by implicit for sparse matrices; pinned explicitly so the
# build is reproducible rather than resolved transitively.
numpy==2.1.3
scipy==1.18.0
implicit==0.7.3
fastapi==0.115.5
uvicorn[standard]==0.32.1
psycopg[binary]==3.2.3
python-dotenv==1.0.1
